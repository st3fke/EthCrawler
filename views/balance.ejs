<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Balance Check Results - Ethereum Transaction Crawler</title>
  <link rel="stylesheet" href="./style/balancestyle.css">
</head>
<body>
  <div class="balance-container">
    <h1>Portfolio Balance</h1>
    
    <div class="balance-summary">
      <h2>Address: <code><%= address %></code></h2>
      <p><strong>Date:</strong> <%= date %></p>
      <p><strong>Block:</strong> #<%= blockNumber %> (<%= blockDate %>)</p>
    </div>

    <!-- ETH Balance -->
    <div class="eth-balance">
      <h2>Ethereum (ETH)</h2>
      <div style="display: flex; justify-content: space-between; align-items: center;">
        <div>
          <div style="font-size: 24px; font-weight: bold;"><%= ethBalance %> ETH</div>
          <% if (ethPrice) { %>
            <div>@ $<%= ethPrice %> per ETH</div>
          <% } %>
        </div>
        <div style="text-align: right;">
          <% if (ethBalanceValue) { %>
            <div style="font-size: 20px; font-weight: bold;">$<%= ethBalanceValue %></div>
          <% } %>
        </div>
      </div>
    </div>

    <!-- Token Balances -->
    <% if (hasTokens) { %>
      <h2>ERC-20 Tokens</h2>
      <div class="token-list">
        <% tokenBalances.forEach(token => { %>
          <div class="token-item">
            <div class="token-info">
              <h3><%= token.symbol %></h3>
              <p><%= token.name %></p>
            </div>
            <div class="token-balance">
              <div class="amount"><%= token.balanceFormatted %> <%= token.symbol %></div>
              <% if (token.price > 0) { %>
                <div class="value">$<%= token.valueFormatted %></div>
              <% } %>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div style="background: #f0f0f0; padding: 20px; border-radius: 8px; text-align: center; color: #666;">
        <p>No ERC-20 tokens found in this wallet at the specified date.</p>
      </div>
    <% } %>

    <!-- Total Portfolio Value -->
    <div class="total-value">
      Total Portfolio Value: $<%= totalPortfolioValue %>
    </div>

    <div style="text-align: center; margin-top: 30px;">
      <a href="/" style="background: #007bff; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px;">
        ← Back to Search
      </a>
    </div>
  </div>
</body>
</html>